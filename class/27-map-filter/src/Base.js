import React from 'react'

export default function Base() {

    /**
     * [필기]
     * # arr.map( callbackFunction, [thisArg]) 분석
     * 
     * - callbackFuntion : 배열의 각 요소를 순회하며 반복 실행 할 함수.
     *  (currentValue, index, array) 3개의 인수를 가짐.
     * 
     * 
     */
  return (
    <div>
      
    </div>
  )
}
